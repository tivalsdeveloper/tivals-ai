(()=>{
const ENDPOINT='https://kxuszpixwfecawdeqkrx.supabase.co/functions/v1/github-agent';
const INSTALL='https://github.com/apps/tivals-ai/installations/new';
const SUPABASE_URL='https://kxuszpixwfecawdeqkrx.supabase.co';
const SUPABASE_KEY='sb_publishable__auyhjNpepXiYdGV5HEJ_A_AGsPbBuS';
const state={repos:[],repo:null,branch:'main',path:'',lastGithubAt:0};
let githubSb=null;
function authClient(){if(window.sb?.auth)return window.sb;if(githubSb?.auth)return githubSb;if(window.supabase?.createClient){githubSb=window.supabase.createClient(SUPABASE_URL,SUPABASE_KEY,{auth:{persistSession:true,autoRefreshToken:true,detectSessionInUrl:true}});return githubSb}return null}
async function session(){try{const c=authClient();if(!c?.auth)return null;const {data}=await c.auth.getSession();return data?.session||null}catch{return null}}
async function waitForSession(timeout=5000){const start=Date.now();while(Date.now()-start<timeout){const s=await session();if(s)return s;await new Promise(r=>setTimeout(r,250))}return null}
async function authHeaders(){const h={'Content-Type':'application/json'},s=await session();if(s?.access_token)h.Authorization='Bearer '+s.access_token;return h}
async function call(payload){const r=await fetch(ENDPOINT,{method:'POST',headers:await authHeaders(),body:JSON.stringify(payload)}),data=await r.json().catch(()=>({success:false,error:'Invalid server response'}));if(!r.ok||data.success===false){const e=new Error(data.error||`GitHub request failed (${r.status})`);e.status=r.status;e.code=data.code;throw e}return data}
async function repos(){const d=await call({action:'list_repos'});state.repos=d.repositories||[];return state.repos}
async function files(full,path='',branchName){const [owner,repo]=full.split('/');return call({action:'list_files',owner,repo,path,branch:branchName||state.branch})}
async function read(full,path,branchName){const [owner,repo]=full.split('/');return call({action:'read_file',owner,repo,path,branch:branchName||state.branch})}
async function branch(full,newBranch,source='main'){const [owner,repo]=full.split('/');return call({action:'create_branch',owner,repo,source_branch:source,new_branch:newBranch})}
async function create(full,path,content,branchName,message){const [owner,repo]=full.split('/');return call({action:'create_file',owner,repo,path,content,branch:branchName||state.branch,message:message||`Create ${path} with Tivals AI`})}
async function update(full,path,content,branchName,message){const [owner,repo]=full.split('/');return call({action:'update_file',owner,repo,path,content,branch:branchName||state.branch,message:message||`Update ${path} with Tivals AI`})}
const toolDefinitions=[
 {name:'github_list_repositories',description:'List repositories available to the signed-in user through the connected Tivals AI GitHub App.',parameters:{type:'object',properties:{},additionalProperties:false}},
 {name:'github_list_files',description:'List files or folders in a connected GitHub repository.',parameters:{type:'object',properties:{repository:{type:'string'},path:{type:'string'},branch:{type:'string'}},required:['repository'],additionalProperties:false}},
 {name:'github_read_file',description:'Read a text file from a connected GitHub repository.',parameters:{type:'object',properties:{repository:{type:'string'},path:{type:'string'},branch:{type:'string'}},required:['repository','path'],additionalProperties:false}},
 {name:'github_create_branch',description:'Create a branch in a connected GitHub repository.',parameters:{type:'object',properties:{repository:{type:'string'},new_branch:{type:'string'},source_branch:{type:'string'}},required:['repository','new_branch'],additionalProperties:false}},
 {name:'github_create_file',description:'Create a text file in a connected GitHub repository. Requires explicit user confirmation before execution.',parameters:{type:'object',properties:{repository:{type:'string'},path:{type:'string'},content:{type:'string'},branch:{type:'string'},message:{type:'string'},confirmed:{type:'boolean'}},required:['repository','path','content','confirmed'],additionalProperties:false}},
 {name:'github_update_file',description:'Replace a text file in a connected GitHub repository. Read the current file first and require explicit user confirmation before execution.',parameters:{type:'object',properties:{repository:{type:'string'},path:{type:'string'},content:{type:'string'},branch:{type:'string'},message:{type:'string'},confirmed:{type:'boolean'}},required:['repository','path','content','confirmed'],additionalProperties:false}}
];
async function executeTool(name,args={}){switch(name){case'github_list_repositories':return repos();case'github_list_files':return files(args.repository,args.path||'',args.branch);case'github_read_file':return read(args.repository,args.path,args.branch);case'github_create_branch':return branch(args.repository,args.new_branch,args.source_branch||'main');case'github_create_file':if(args.confirmed!==true)throw new Error('User confirmation is required before creating a GitHub file.');return create(args.repository,args.path,args.content,args.branch,args.message);case'github_update_file':if(args.confirmed!==true)throw new Error('User confirmation is required before updating a GitHub file.');await read(args.repository,args.path,args.branch);return update(args.repository,args.path,args.content,args.branch,args.message);default:throw new Error('Unknown GitHub tool: '+name)}}
function modelContext(){return {connected:true,current_repository:state.repo,current_branch:state.branch,tools:toolDefinitions,instructions:'GitHub access is provided by Tivals AI, not by the language model provider. Never claim GitHub is inaccessible merely because the selected model cannot access external services directly. Use the shared TivalsGitHub tool layer for repository operations. Read existing files before proposing replacements. Never execute create/update operations without explicit user confirmation.'}}
function githubIntent(q=''){const direct=/\bgithub\b/i.test(q)||/\b(repositor(?:y|ies)|repos?)\b/i.test(q)||/\b(list|show|read|open|update|edit|change|modify|create|add|fix)\b.*\b(files?|branches?|code|repo(?:sitory)?)\b/i.test(q);const follow=state.repo&&Date.now()-state.lastGithubAt<30*60*1000&&/^(update|edit|change|modify|fix|create|add|read|show|list|open)\b/i.test(q.trim());return direct||follow}
function repoFrom(q=''){const full=q.match(/\b([A-Za-z0-9_.-]+\/[A-Za-z0-9_.-]+)\b/);if(full)return full[1];return state.repo||''}
function listFromData(d){return d.files||d.items||d.entries||d.contents||d.content||[]}
async function handlePrompt(q=''){q=String(q).trim();if(!githubIntent(q))return{handled:false};state.lastGithubAt=Date.now();if(/\b(list|show|what|which)\b.*\b(repositor(?:y|ies)|repos?)\b/i.test(q)||/\bmy\s+github\b/i.test(q)){const rs=await repos();return{handled:true,text:rs.length?'Your connected GitHub repositories:\n\n'+rs.map(r=>'- '+(r.full_name||r.name)).join('\n'):'No repositories were returned by your GitHub connection.'}}const repo=repoFrom(q);if(!repo)return{handled:true,text:'Tell me which GitHub repository to use, for example: `tivalsdeveloper/tivals-ai`.'};state.repo=repo;if(/\b(list|show)\b.*\bfiles?\b/i.test(q)){const d=await files(repo,''),xs=listFromData(d);return{handled:true,text:xs.length?`Files in ${repo}:\n\n`+xs.map(x=>'- '+(x.path||x.name)).join('\n'):`No files were returned for ${repo}.`}}if(/^(update|edit|change|modify|fix|create|add)\b/i.test(q)||/\b(update|edit|change|modify|fix|create|add)\b.*\b(file|code|repo)/i.test(q)){return{handled:true,text:`I’m connected to **${repo}** through the shared GitHub tool layer. All Tivals AI models can use this connection. Tell me the exact file/change you want; write operations require confirmation before commit.`}}return{handled:false}}
function show(role,text){if(typeof window.add==='function')return window.add(role,text);console.log('[Tivals GitHub]',role,text)}
async function bindInstallation(id){const s=await waitForSession();if(!s)return false;try{const d=await call({action:'connect_installation',installation_id:Number(id)});show('ai',`✓ GitHub connected${d.account_login?' as '+d.account_login:''}. All Tivals AI models can now use the shared GitHub tools.`);return true}catch(e){console.error('[Tivals GitHub] bind failed',e);return false}}
async function openConnect(){const s=await waitForSession(1500);if(!s){show('ai','Please sign in to Tivals AI before connecting GitHub.');return}sessionStorage.setItem('tivals-github-return',location.href.split('?')[0]);location.href=INSTALL}
function addConnectButton(){if(document.querySelector('#githubConnect'))return;const host=document.querySelector('.sideTools')||document.querySelector('.tools');if(!host)return;const b=document.createElement('button');b.id='githubConnect';b.type='button';b.className=host.classList.contains('sideTools')?'sideTool':'toolBtn';b.textContent='Connect GitHub';b.onclick=openConnect;host.appendChild(b)}
function cleanInstallParams(){const p=new URLSearchParams(location.search);p.delete('installation_id');p.delete('setup_action');history.replaceState({},'',location.pathname+(p.toString()?'?'+p:'')+location.hash)}
async function finishInstall(){const p=new URLSearchParams(location.search),id=p.get('installation_id');if(!id)return;const s=await waitForSession();if(!s){show('ai','Your GitHub App returned successfully, but your Tivals AI session has expired. Sign in, then tap Connect GitHub again.');return}if(await bindInstallation(id)){cleanInstallParams();return}show('ai','GitHub returned successfully, but the installation could not be linked. Tap Connect GitHub and try once more.')}
let running=false;async function intercept(e){const send=e.type==='click'?e.target.closest?.('#send'):e.type==='keydown'&&e.key==='Enter'&&!e.shiftKey&&e.target?.matches?.('#prompt');if(!send||running)return;const box=document.querySelector('#prompt'),q=box?.value?.trim();if(!q||!githubIntent(q))return;e.preventDefault();e.stopImmediatePropagation();running=true;box.value='';show('user',q);const wait=show('ai','Using shared GitHub tools…');try{const out=await handlePrompt(q);if(!out.handled)throw new Error('I need a repository or file action.');wait?.remove?.();show('ai',out.text)}catch(err){wait?.remove?.();if(err?.status===401)show('ai','GitHub is not connected to this Tivals AI account. Open the menu and tap **Connect GitHub**, then authorize the Tivals AI GitHub App.');else show('ai','GitHub connection error: '+(err.message||err))}finally{running=false}}
document.addEventListener('click',intercept,true);document.addEventListener('keydown',intercept,true);async function boot(){addConnectButton();await finishInstall()}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot);else boot();window.TivalsGitHub={call,repos,files,read,branch,create,update,openConnect,state,tools:toolDefinitions,executeTool,modelContext,handlePrompt};window.dispatchEvent(new CustomEvent('tivals-github-tools-ready',{detail:{tools:toolDefinitions}}));
})();